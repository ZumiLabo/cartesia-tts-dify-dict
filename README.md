## cartesia-tts-dict

Cartesia TTS plugin for Dify with support for `pronunciation_dict_id` and Sonic 3.

### Added features
- `pronunciation_dict_id` credential field
- `model_id` configurable from Dify model setting
- default/fallback model set to `sonic-3`
- basic Japanese-friendly sentence splitting
